# Python package requirements for the "Just Drop the Subject" project
# PyTorch should be installed separately via its official instructions
# to ensure CUDA compatibility for your specific hardware (P6000/A1000).
# e.g., pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# === Core ML & Training ===
transformers==4.41.2
datasets==2.19.2
sentencepiece==0.2.0
protobuf==4.25.3
wandb==0.17.0

# === Linguistic Preprocessing (Ablations) ===
# Added for all ablation procedures (POS, dependency, lemma, etc.)
spacy==3.7.5
# Note: After installing, you must download the necessary models, e.g.:
# python -m spacy download en_core_web_trf

# === Data Validation & CLI ===
pydantic==2.7.4
typer[all]==0.12.3
pyyaml==6.0.1

# === Utilities ===
tqdm==4.66.4
numpy==1.26.4
nltk==3.8.1